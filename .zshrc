function mkcd {
  mkdir -p "$1" && cd "$1"
}

alias norm='norminette'

alias glibft='gcc -L. -lft'

alias gall='gcc -Wall -Wextra -Werror'

alias 42test='bash ~/42FileChecker/42FileChecker.sh'

alias grademe='bash ~/Libftest/grademe.sh'
